name: 2. Application Pipeline

on:
  push:
    paths:
      - 'app/**'
  workflow_dispatch:

env:
  ECR_REGISTRY: 437797167825.dkr.ecr.us-east-1.amazonaws.com
  ECR_REPOSITORY: vulnerable-app
  IMAGE_TAG: ${{ github.sha }}

jobs:
  security-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # SAST
      - name: Run SonarQube SAST Scan
        uses: SonarSource/sonarqube-scan-action@master
        continue-on-error: true
        with:
          projectBaseDir: ${{ github.workspace }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      # SCA
      - name: Run Snyk Dependency Scan
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --json-file-output=${{ github.workspace }}/app/snyk-report.json --severity-threshold=high
        continue-on-error: true

      # Build image once - reuse for Trivy and deploy
      - name: Build Container Image
        run: docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

      # Container scan
      - name: Run Trivy Container Scan
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy image \
            --format json \
            --output ${{ github.workspace }}/app/trivy-image-report.json \
            $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || true

      # IaC scan (report only)
      - name: Run Checkov IaC Scan
        run: |
          docker run --rm -v ${{ github.workspace }}/terraform:/tf \
            bridgecrew/checkov -d /tf --output json > ${{ github.workspace }}/app/checkov-report.json || true

      # DAST - note port 3000, not 5000
      - name: Start MySQL for DAST
        run: |
          docker network create appnet
          docker run -d --network appnet --name mysql \
            -e MYSQL_ROOT_PASSWORD=test \
            -e MYSQL_DATABASE=appdb \
            -e MYSQL_USER=admin \
            -e MYSQL_PASSWORD=test \
            mysql:8.0
          sleep 20

      - name: Start App for DAST
        run: |
          docker run -d --network appnet -p 3000:3000 --name local-app \
            -e DB_HOST=mysql \
            -e DB_USER=admin \
            -e DB_PASSWORD=test \
            -e DB_NAME=appdb \
            -e DB_PORT=3306 \
            $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          sleep 15

      - name: Run OWASP ZAP DAST Scan
        run: |
          docker run --rm --network appnet \
            -v ${{ github.workspace }}/app:/zap/wrk/:rw \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py -t http://local-app:3000 \
            -J zap-report.json || true

      # Push to ECR
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Push Image to ECR
        run: |
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
                     $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest


      - name: Debug - Global Runner Search
        run: |
          echo "=== Searching Entire Runner Filesystem for Exact Reports ==="
          sudo find / -type f \( -name "snyk-report.json" -o -name "trivy-image-report.json" -o -name "zap-report.json" -o -name "checkov-report.json" \) 2>/dev/null -exec ls -lh {} \;


      # Upload all reports to DefectDojo
      - name: Upload Snyk to DefectDojo
        run: |
          curl -X POST "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_TOKEN }}" \
            -F "scan_type=Snyk Scan" \
            -F "engagement=1" \
            -F "file=@${{ github.workspace }}/app/snyk-report.json"

      - name: Upload Trivy to DefectDojo
        run: |
          curl -X POST "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_TOKEN }}" \
            -F "scan_type=Trivy Scan" \
            -F "engagement=1" \
            -F "file=@${{ github.workspace }}/app/trivy-image-report.json"

      - name: Upload Checkov to DefectDojo
        run: |
          curl -X POST "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_TOKEN }}" \
            -F "scan_type=Checkov Scan" \
            -F "engagement=1" \
            -F "file=@${{ github.workspace }}/checkov-report.json"

      - name: Upload ZAP to DefectDojo
        run: |
          curl -X POST "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_TOKEN }}" \
            -F "scan_type=ZAP Scan" \
            -F "engagement=1" \
            -F "file=@${{ github.workspace }}/app/zap-report.json"
